package Views;

import Controllers.BaseController;

public class MainWindow extends BaseWindow{
    public MainWindow(int w, int h, BaseController controller) {
        super(w, h, controller);
    }
}